{
  "name": "Remove `enum=[null]` if `type=['null']`",
  "in": {
    "type": "object",
    "properties": {
      "noopMissingType": {
        "enum": ["foo", "bar"]
      },
      "noopMissingEnum": {
        "type": ["null", "string"]
      },
      "noopNonNullableEnum": {
        "type": "string",
        "enum": ["foo", "bar", null]
      },
      "dedupeNulls": {
        "type": ["null", "string"],
        "enum": ["foo", "bar", null]
      }
    }
  },
  "out": {
    "additionalProperties": true,
    "id": "RedundantNull",
    "readOnly": false,
    "required": [],
    "type": "object",
    "properties": {
      "noopMissingType": {
        "readOnly": false,
        "enum": ["foo", "bar"]
      },
      "noopMissingEnum": {
        "readOnly": false,
        "type": ["null", "string"]
      },
      "noopNonNullableEnum": {
        "readOnly": false,
        "type": "string",
        "enum": ["foo", "bar", null]
      },
      "dedupeNulls": {
        "readOnly": false,
        "type": "string",
        "enum": ["foo", "bar", null]
      }
    }
  }
}
